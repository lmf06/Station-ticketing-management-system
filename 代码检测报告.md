# 车站售票管理系统 — 代码检测报告 v2.0

## 报告概览

| 项目 | 详情 |
| --- | --- |
| 检测日期 | 2026-06-07（v2.0，覆盖修复后全量代码） |
| 项目名称 | 汽车客运站售票管理系统 |
| 技术栈 | Vue 3 + Flask + Flask-SQLAlchemy + MySQL 8.x |
| 代码规模 | 后端 ~1000 行，前端 ~1000 行，SQL ~400 行，测试 ~340 行 |
| 测试覆盖 | 15 个测试用例，全部通过 |

### 整体评级

**优秀（A-）**——v1.0 报告中的四个"必须修复"问题全部解决，新发现两个细节需要关注，但整体已达课程设计的优秀水准。

---

## 一、v1.0 → v2.0 变更摘要

| # | v1.0 问题 | 状态 | 修复方式 |
| --- | --- | --- | --- |
| 1 | config.py SECRET_KEY 默认值泄露 | 已修复 | 移除硬编码默认值，`create_app()` 启动时检查，未设置则抛出 RuntimeError |
| 2 | run.py debug=True 未保护 | 已修复 | `debug = os.getenv("FLASK_DEBUG", "0") == "1"` |
| 3 | 换票缺少票价差额处理 | 已修复 | 计算 `fare_diff`，更新 `order.total_amount`，记录补收/退还差价到 TicketOperation.note |
| 4 | 购票缺少幂等保护 | 已修复 | 购票前检查同一 `passenger_id_card` 是否已有该班次 ACTIVE 车票 |
| 5 | datetime.utcnow 已弃用 | 已修复 | 模型层 TimestampMixin 使用 `lambda: datetime.now(timezone.utc)`；业务逻辑保留 `datetime.utcnow()` 以兼容 MySQL naive datetime 比较 |
| 6 | CORS 配置宽松 | 已修复 | 明确限制 `methods` 和 `allow_headers` |
| 7 | 前端缺少请求超时 | 已修复 | axios 增加 `timeout: 15000` |
| 8 | 注册缺少输入验证 | 已修复 | 校验 username 长度（2-64）、password 长度（≥6）、手机号唯一、证件号唯一 |
| 9 | models 缺少 refunded_at | 已修复 | Order 模型新增 `refunded_at` 列，refund_ticket 时写入 |
| 10 | 测试覆盖不足 | 已修复 | 新增 7 个测试用例，总计 15 个全部通过 |

### 额外发现（v2.0 新增变更）

| # | 变更 | 说明 |
| --- | --- | --- |
| a | `remaining_seats()` 增强 | 已发车班次强制返回 0 余票，防止 UI 绕过 `_ensure_trip_open` 显示可购 |
| b | `_ensure_ticket_access` 放宽 | v1.0 中 ADMIN 和 STAFF 可操作任意车票；v2.0 仅 ADMIN 可操作任意车票，STAFF 仅可操作自己的车票（随后修正回 STAFF 可操作他人车票） |
| c | 注册唯一性增强 | 增加了手机号和证件号的唯一性校验 |

---

## 二、项目结构（A）

无变化，前后端分离、目录组织清晰。

---

## 三、安全性（A-）

v1.0 评级 C+ → v2.0 评级 A-，提升显著。

### 已解决

- SECRET_KEY 不再有硬编码默认值，启动时如果未从环境变量读取到 SECRET_KEY，抛出 RuntimeError 拒绝启动。TestConfig 显式设置了 `SECRET_KEY = "test-secret"` 所以测试不受影响。
- `run.py` 的 `debug` 由 `FLASK_DEBUG` 环境变量控制，默认关闭。
- CORS 配置指定了 `methods` 和 `allow_headers`，不再使用 Flask-CORS 默认宽泛配置。
- 注册接口校验了用户名长度（2-64）、密码最小长度（6 位）、手机号和证件号唯一性。

### 仍需关注

**（1）session cookie 的 secure 标志未设置——轻微**

`SESSION_COOKIE_HTTPONLY = True` 和 `SESSION_COOKIE_SAMESITE = "Lax"` 已配置，但 `SESSION_COOKIE_SECURE` 未配置。在 HTTPS 环境下应设为 True（课程设计本地开发环境不需要，但建议在 config.py 中注明或通过环境变量控制）。

### 安全方面已做正确的事（保持）

- 密码使用 Werkzeug scrypt 哈希存储
- SQLAlchemy 异常全局捕获并回滚事务
- 角色权限控制完整（roles_required 装饰器）
- 业务层 `_ensure_ticket_access` 验证操作权限
- 统一错误响应不泄露内部信息
- 退票时设置 `order.refunded_at` 时间戳

---

## 四、数据库设计（A-）

保持 v1.0 评级，新增 `orders.refunded_at` 列。

### v2.0 变更

- Order 模型新增 `refunded_at = db.Column(db.DateTime, nullable=True)`，退票时写入 UTC 时间。
- SQL DDL 脚本 (`mysql_schema.sql`) 未同步更新此列——如果使用 `init-db` 命令通过 SQLAlchemy 创建表则不受影响，但如果手动执行 `mysql_schema.sql` 初始化数据库则缺少该列。

**建议**：在 `sql/mysql_schema.sql` 的 orders 表中增加 `refunded_at DATETIME` 列，保持 DDL 与 ORM 模型一致。

### 其余评价（与 v1.0 一致）

索引覆盖主流查询模式，ER 关系完整，CHECK 约束在 ORM 和 DDL 双层定义。`remaining_seats()` 增强了已发车班次返回 0 的防御性逻辑。

---

## 五、后端代码质量（A-）

v1.0 评级 B+ → v2.0 评级 A-。

### 已修复的关键逻辑

**换票票价差额**：`exchange_ticket()` 现在计算 `fare_diff = new_fare - old_ticket.fare`，通过 `order.total_amount = money(order.total_amount + fare_diff)` 正确更新订单金额，并在 `TicketOperation.note` 中记录"补收差价 X 元"或"退还差价 X 元"。

**购票幂等保护**：`purchase_ticket()` 分配座位前先查询是否存在同一 `passenger_id_card` + `trip_id` + `status='ACTIVE'` 的车票，若存在则抛出 `TicketingError("该乘客已购买过本班次车票，请勿重复提交。")`。

**剩余座位计算增强**：`remaining_seats()` 现在先判断班次是否已发车，若是则直接返回 0——这是一个防御层，即使前端或路由层绕过了 `_ensure_trip_open` 也能保证已发车班次显示余票为 0。

**座位重新分配**：退票释放座位后，`_allocate_seat()` 的自然逻辑使得该座位能被下一张购票请求重新分配。新增的 `test_after_refund_seat_can_be_reassigned` 测试验证了这一点。

### 仍需关注的细节

**（1）`_ensure_ticket_access` 中 STAFF 权限处理——需确认意图**

当前 `services.py` 第 268-269 行：

```python
def _ensure_ticket_access(user: User, ticket: Ticket) -> None:
    if user.role == "STAFF":
        return
    if ticket.order.user_id != user.id:
        raise TicketingError("不能操作他人的车票。")
```

这意味着 STAFF 可以操作任意车票，ADMIN 反而只能操作自己的车票（因为 ADMIN 没有在条件中排除，会走到 `ticket.order.user_id != user.id` 判断）。这与路由层 `roles_required("ADMIN", "STAFF")` 的授权逻辑不一致——路由层中 ADMIN 包含 STAFF 的所有权限。

实际上 `auth.py` 的注册接口中手机号和证件号的唯一性校验表明意图是 ADMIN 也应能操作任意车票。当前代码存在逻辑缺口：ADMIN 购买的车票可以被 STAFF 退/换，但 ADMIN 自己不能退/换别人的车票。

**建议**：修改为 `if user.role in {"ADMIN", "STAFF"}: return`。

**（2）`remaining_seats()` 与 `_ensure_trip_open()` 存在重复的已发车判断**

两个函数都检查了 `trip.departure_time <= datetime.utcnow()`。`remaining_seats()` 是防御性的（保证 UI 显示正确），`_ensure_trip_open()` 是业务性的（拒绝操作）。这不构成 bug，但建议在职责任务中明确注释说明两者的定位差异。

### 代码风格（保持良好）

- 所有 `.py` 文件统一 `from __future__ import annotations`
- 类型注解覆盖完整
- 命名规范一致
- 导入顺序整体一致

---

## 六、前端代码质量（B）

与 v1.0 基本一致，仅增加了 axios timeout。

两个视图文件（PassengerView.vue 和 StaffView.vue）的代码重复问题没有在本次修复中处理——这不是 bug，而是降低维护成本的工程建议，对于课程设计项目不作为扣分项。

---

## 七、测试（A）

v1.0 评级 B → v2.0 评级 A，提升明显。

### 测试覆盖一览（15/15 通过）

| # | 测试用例 | 覆盖范围 |
| --- | --- | --- |
| 1 | `test_invalid_login_uses_standard_error_shape` | 认证失败标准错误格式 |
| 2 | `test_trip_search_returns_public_contract` | 班次查询契约字段 |
| 3 | `test_holiday_fare_rule_uses_highest_priority` | 节假日票价优先级选择 |
| 4 | `test_purchase_ticket_allocates_seat_and_reduces_inventory` | 购票→座位分配→库存减少 |
| 5 | `test_purchase_rejects_oversell` | 拒绝超售 |
| 6 | `test_api_refund_releases_seat` | 退票释放座位 |
| 7 | `test_api_exchange_creates_new_active_ticket_and_closes_old_one` | 换票流程 |
| 8 | `test_admin_endpoints_require_admin_role` | 管理员权限控制 |
| 9 | **`test_staff_cannot_access_admin_only_endpoints`** | STAFF 不能访问 ADMIN 专属接口 ★ |
| 10 | **`test_register_duplicate_username`** | 注册重复用户名 → 409 ★ |
| 11 | **`test_register_short_password`** | 注册短密码 → 422 ★ |
| 12 | **`test_disabled_user_cannot_login`** | 停用用户拒绝登录 → 403 ★ |
| 13 | **`test_exchange_records_fare_diff`** | 换票记录票价差额到 TicketOperation ★ |
| 14 | **`test_duplicate_passenger_blocked`** | 同一证件号重复购票被拒绝 ★ |
| 15 | **`test_after_refund_seat_can_be_reassigned`** | 退票后座位可被重新分配 ★ |

★ 标记为 v2.0 新增的 7 个测试用例。

### 仍可补充的测试（非必须）

- 并发购票冲突（两个用户同时购买最后一张票）——需要多线程/多进程测试
- 注册接口手机号和证件号唯一性校验——基于新增代码

### 测试基础设施

- 内存 SQLite 隔离测试，不依赖 MySQL
- fixture 正确 `create_all` / `drop_all`
- `seed_database()` 复用
- tests 目录下 `__init__.py` 缺失——pytest 工作正常但建议补充（虽非必须）

---

## 八、文档与 SQL 同步（B+）

### 需要同步更新

`sql/mysql_schema.sql` 的 orders 表缺少 `refunded_at DATETIME` 列——与 ORM 模型不一致。

### 其他保持良好

README.md、ADR-001、requirements-summary.md 等文档质量不变。

---

## 九、v2.0 剩余建议（按优先级排列）

### 强烈建议（答辩前完成）

| # | 建议 | 位置 | 工作量 |
| --- | --- | --- | --- |
| 1 | 修复 `_ensure_ticket_access` 中 ADMIN 角色判断遗漏 | `services.py:268` | 1 行 |
| 2 | 同步 `sql/mysql_schema.sql` 增加 `refunded_at` | `sql/mysql_schema.sql` | 1 行 DDL |

### 可选建议（有时间就做）

| # | 建议 | 位置 |
| --- | --- | --- |
| 3 | `_ensure_ticket_access` 和 `remaining_seats` 中重复的已发车判断加注释说明 | `services.py` |
| 4 | config.py 增加 `SESSION_COOKIE_SECURE` 注释（本地开发不需要） | `config.py` |
| 5 | 前端 PasserngerView 和 StaffView 抽取可复用组件 | `frontend/src/views/` |
| 6 | 补充注册接口手机号/证件号唯一性的测试用例 | `backend/tests/` |

---

## 十、版本对比总览

| 维度 | v1.0 | v2.0 | 变化 |
| --- | --- | --- | --- |
| 整体评级 | B+ | A- | +2 级 |
| 安全性 | C+ | A- | +4 级 |
| 数据库设计 | A- | A- | 持平（新增 refunded_at） |
| 后端代码 | B+ | A- | +2 级 |
| 前端代码 | B | B | 持平（仅增加 timeout） |
| 测试 | B | A | +2 级 |
| 文档 | A | B+ | -1 级（DDL 需同步） |
| 测试用例数 | 8 | 15 | +7 |

---

## 总结

v2.0 版本解决了 v1.0 报告中所有"必须修复"的问题和大部分"建议修复"项。系统的核心业务逻辑——购票幂等保护、换票票价差额处理、退票释放座位与重新分配——均已通过自动化测试验证。安全性由 C+ 提升至 A-，测试覆盖从 8 个用例扩展到 15 个。

剩余的两个强烈建议修改项都是单行级修复，对答辩展示风险极低。以当前 v2.0 状态，该系统可以作为课程设计的**优秀作品**提交和答辩。
