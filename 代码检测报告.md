# 车站售票管理系统 — 代码检测报告

## 报告概览

| 项目 | 详情 |
| --- | --- |
| 检测日期 | 2026-06-07 |
| 项目名称 | 汽车客运站售票管理系统 |
| 技术栈 | Vue 3 + Flask + Flask-SQLAlchemy + MySQL 8.x |
| 代码规模 | 后端 ~900 行，前端 ~1000 行，SQL 脚本 ~400 行，测试 ~200 行 |
| 检测范围 | 全部源代码文件（排除 node_modules、.venv、dist） |

### 整体评级

**良好（B+）**——系统核心功能完整、架构清晰，但存在若干安全风险和代码质量问题需要在答辩前修正。

---

## 一、项目结构（A）

目录组织合理，前后端分离，文档、SQL 脚本和测试独立放置。

```
车站售票管理系统/
├── backend/
│   ├── app/
│   │   ├── __init__.py          # Flask 工厂函数
│   │   ├── config.py            # 配置管理
│   │   ├── extensions.py        # Flask 扩展实例
│   │   ├── models.py            # SQLAlchemy 模型（12 张表）
│   │   ├── services.py          # 核心业务逻辑
│   │   ├── seed.py              # 种子数据
│   │   └── routes/
│   │       ├── admin.py         # 管理端接口
│   │       ├── auth.py          # 认证接口
│   │       ├── orders.py        # 购票接口
│   │       ├── tickets.py       # 退票/换票接口
│   │       ├── trips.py         # 班次查询接口
│   │       └── common.py        # 通用工具
│   ├── tests/
│   │   ├── test_api_contract.py # API 契约测试
│   │   └── test_ticketing.py    # 票务逻辑测试
│   ├── requirements.txt
│   └── run.py
├── frontend/src/
│   ├── api.js                   # Axios 封装
│   ├── main.js                  # 入口
│   ├── router.js                # 路由守卫
│   ├── stores/auth.js           # Pinia 认证状态
│   ├── views/
│   │   ├── LoginView.vue        # 登录/注册
│   │   ├── PassengerView.vue    # 旅客购票
│   │   ├── StaffView.vue        # 窗口业务
│   │   └── AdminView.vue        # 系统管理
│   └── assets/main.css
├── sql/
│   ├── mysql_schema.sql         # 建表脚本
│   ├── mysql_seed.sql           # 种子数据
│   └── mysql_queries.sql        # 事务示例
├── docs/
│   ├── course/requirements-summary.md
│   ├── decisions/ADR-001-architecture.md
│   ├── midterm-report-content.md
│   └── report-outline.md
├── .env
├── .gitignore
├── README.md
└── pytest.ini
```

结果：结构清晰，职责分明，符合课程设计规范要求。

---

## 二、安全性（C+）

### 2.1 严重问题

**（1）SECRET_KEY 硬编码泄露——严重**

文件 `backend/app/config.py` 中：

```python
SECRET_KEY = os.getenv("SECRET_KEY", "dev-secret-change-me")
```

文件 `.env` 中：

```
SECRET_KEY=station-local-dev
```

虽然 `.env` 在 `.gitignore` 中，但 `config.py` 中的默认值 `"dev-secret-change-me"` 与 `.env` 中的值 `"station-local-dev"` 均被提交到了代码中。`config.py` 的默认密钥任何人都能看到（即便 `.gitignore` 已添加 `.env`，`config.py` 中的硬编码后备值仍然是公开的）。

**建议**：删除 `config.py` 中的默认值，在启动时强制从环境变量读取，若未设置则拒绝启动。`.env` 不慎提交到了仓库中——检查 Git 历史记录，确认 `station-local-dev` 是否已经泄露。

**（2）debug=True 在生产式代码中未保护**

文件 `backend/run.py`：

```python
app.run(host="127.0.0.1", port=5000, debug=True)
```

`debug=True` 会暴露 Werkzeug 调试器，允许在浏览器中执行任意 Python 代码。虽然是课程设计项目且绑定到 localhost，但仍需警惕。

**建议**：改为 `debug=os.getenv("FLASK_DEBUG", "0") == "1"`，从环境变量控制。

**（3）密码哈希未限制输入长度**

文件 `backend/app/models.py` 中 `User.set_password` 和 `auth.py` 的注册接口均未对密码长度设限。

**建议**：在注册接口处限制密码最小长度（如 6 位），`username` 也应在注册时验证格式。

### 2.2 中等问题

**（4）CORS 配置宽松**

```python
CORS_ORIGINS = [origin.strip() for origin in os.getenv("CORS_ORIGINS", "http://127.0.0.1:5173,http://localhost:5173").split(",")]
```

虽然通过环境变量控制还算合理，但未限制 `Access-Control-Allow-Headers` 和 `Access-Control-Allow-Methods`，使用了 Flask-CORS 的默认值。

**建议**：明确指定 `allow_headers` 和 `methods`。

**（5）敏感信息通过日志泄漏风险**

`auth.py` 和 `orders.py` 中异常被 `ElMessage.error(error.message)` 直接展示给前端，但没有在服务端做统一的异常信息脱敏处理。例如 `TicketingError("班次不存在。")` 虽然无害，但若数据库异常被捕获并返回到前端，可能泄漏表结构信息。

**建议**：`SQLAlchemyError` 处理器已正确返回通用错误信息，这一点做得不错。但需要确保所有自定义异常在路由层都被捕获并返回用户友好的错误。

### 2.3 已做正确的事项

- SQLAlchemy 异常全局捕获并回滚事务——`__init__.py` 中 `handle_sqlalchemy_error` 正确地调用了 `db.session.rollback()` 并返回通用错误。
- 密码使用 Werkzeug 的 `generate_password_hash`（scrypt）存储——合格。
- Session cookie 设置了 `HttpOnly` 和 `SameSite=Lax`——防护 CSRF。
- 路由层 `roles_required` 装饰器正确验证权限——管理员端点被正确保护。
- 业务层 `_ensure_ticket_access` 验证旅客只能操作自己的车票——正确。

---

## 三、数据库设计（A-）

### 3.1 优点

- 12 张表，覆盖用户、旅客档案、站点、线路、经停站、车辆、座位、班次、票价规则、订单、车票、操作日志——设计完整。
- 每张表都定义了 PRIMARY KEY、FOREIGN KEY、CHECK 约束和合理的 INDEX。
- `route_stops` 表预留了经停站的扩展能力。
- `ticket_operations` 表记录了完整的票务审计追踪。
- SQLAlchemy ORM 模型与 SQL DDL 一致，CHECK 约束重复定义在两层（ORM 层和数据库层）——防御性设计。
- `fare_rules` 表支持按日期范围和优先级生效，灵活且设计合理。
- SQL 脚本中的事务示例直接利用 `SELECT ... FOR UPDATE` 做行级锁——业务理解准确。

### 3.2 需要关注的问题

**（1）SQLAlchemy 模型缺少外键命名**

SQLAlchemy 模型中的 `ForeignKey` 均未指定 `name` 参数。MySQL 中会自动生成外键名，但在 SQLAlchemy 层面，migration 工具（如 Alembic）依赖外键名做变更。当前项目没有使用 Alembic，暂时影响不大，但若未来需要数据库迁移会带来麻烦。

**（2）Seat 表的 vehicle_id 在 Trip 创建时无校验**

创建 Trip 时没有在校验中确保 `vehicle.seat_count == len(vehicle.seats)`，如果管理员创建车辆后忘记同步创建 Seat 记录，`_allocate_seat` 会回退到 `[str(i) for i in range(1, seat_count + 1)]` 的方案——这是一个很好的防御性设计，已在代码中实现。

**（3）`orders` 表缺少 `refunded_at` 时间戳**

目前只有 `paid_at`，退票和换票的时间只能从 `ticket_operations` 表中获取。在 `orders` 表中增加 `refunded_at` 字段会便于统计查询。

**（4）`fare_rules` 的 `multiplier` 字段使用 DECIMAL(5,2)**

这意味着倍率最大为 999.99（5 位中 2 位小数），对于节假日浮动足够，但如果有长期折扣（如 0.75 折），建议确认业务上是否永远在 0.01~999.99 范围内。

### 3.3 索引分析

当前索引覆盖了最常见的查询模式：

- `ix_trips_departure_time`——班次按时间查询
- `ix_trips_route_departure`——联合索引，按线路和时间查班次
- `ix_tickets_trip_status`——按班次和状态统计已售座位
- `ix_tickets_order`——按订单查车票
- `ix_orders_user_status`——按用户和状态查订单
- `ix_fare_rules_date_range`——按日期范围查票价规则

缺少的索引：

- `tickets` 表缺少 `(trip_id, status, seat_number)` 的覆盖索引——`_allocate_seat` 中的 `SELECT seat_number FROM tickets WHERE trip_id=? AND status='ACTIVE'` 需要使用 `ix_tickets_trip_status` 再回表取 `seat_number`。对于高并发售票场景，添加覆盖索引会提升性能。在课程设计的数据量级下影响可忽略。

---

## 四、后端代码质量（B+）

### 4.1 优点

- Flask 工厂模式——`create_app` 支持配置注入，测试用 `TestConfig` 隔离。
- 服务层分离——`services.py` 封装核心业务逻辑（购票、退票、换票），路由层薄薄一层。
- 统一错误响应格式——`api_error()` 和 `error()` 保证所有错误返回 `{"error": {"code": "...", "message": "..."}}` 格式。
- `TicketingError` 自定义异常类——业务错误与系统错误分离。
- `money()` 函数使用 `Decimal` 做精确金额计算——避免浮点数误差。
- `_lock_trip` 和 `_lock_ticket` 使用 `SELECT ... FOR UPDATE`——在 MySQL 下正确实现悲观锁。
- SQLite 兼容性——`_lock_trip` 中判断方言，在 SQLite（测试环境）下跳过行锁。

### 4.2 需要修正的问题

**（1）购票缺少幂等保护——中等**

```python
def purchase_ticket(user, trip_id, passenger_name, passenger_id_card):
    trip = _lock_trip(trip_id)
    _ensure_trip_open(trip)
    seat_number = _allocate_seat(trip)
    # ...
```

如果网络重试导致同一个 POST 请求到达两次，`_lock_trip` 的 `FOR UPDATE` 锁在同一个事务内只会加一次锁（已持有），不会阻止第二次插入。这意味着同一乘客可能被分配两个座位。

**建议**：在 tickets 表上增加 `UNIQUE (trip_id, passenger_id_card, status='ACTIVE')` 的部分唯一索引（MySQL 不支持部分索引，可用触发器或应用层检查替代），或在购买前检查该乘客是否已有同一班次的有效车票。

**（2）换票中票价差额未处理——中等**

```python
def exchange_ticket(operator, ticket_id, new_trip_id):
    # ...
    old_ticket.order.total_amount = money(new_fare)
    # ...
```

换票时直接将订单总金额更新为新票价，未计算差额（`new_fare - old_fare`），也没有在 `TicketOperation` 中记录补退差额。这意味着：
- 从低价票换到高价票时，系统未向用户收取差价；
- 从高价票换到低价票时，系统未退还差价。

**建议**：计算 `fare_diff = new_fare - old_ticket.fare`，若 `fare_diff > 0` 则要求补款，若 `fare_diff < 0` 则记录退款。同时更新 `order.total_amount` 为正确的累计值。

**（3）`sales_stats` 聚合查询可能不准确——轻微**

```python
def sales_stats():
    active_sales = db.session.query(func.coalesce(func.sum(Ticket.fare), 0))
        .filter(Ticket.status == "ACTIVE").scalar()
```

这个查询统计的是所有 ACTIVE 车票的票价总和。但如果一次换票中，新车票票价与原车票票价不同，`order.total_amount` 已被更新为新车票票价，而两张车票的 fare 之和可能与 order 的 total_amount 不一致。

**（4）`datetime.utcnow` 已弃用——轻微**

SQLAlchemy 模型中使用了 `datetime.utcnow`，Python 3.12+ 建议使用 `datetime.now(datetime.UTC)`。

**建议**：替换为 `datetime.now(datetime.UTC)` 或使用 SQLAlchemy 的 `func.now()`。

**（5）`_build_no` 在高并发下有碰撞风险——轻微**

```python
def _build_no(prefix):
    return f"{prefix}{datetime.utcnow():%Y%m%d%H%M%S}{uuid4().hex[:8].upper()}"
```

在同一秒内大量并发请求可能产生相同前缀，虽然后缀有 8 位十六进制随机数（约 4.3 billion 种可能），碰撞概率极低。数据库有 UNIQUE 约束兜底——如果碰撞发生，会触发 IntegrityError。建议在 `purchase_ticket` 中捕获该异常并重试。

**（6）`User.is_active` 属性注意事项——正确但需留意**

```python
@property
def is_active(self):
    return bool(self.is_active_flag)
```

Flask-Login 依赖 `is_active` 属性判断用户是否被禁用——这里用 `@property` 覆盖了 `UserMixin.is_active`，且正确地委托给 `is_active_flag` 列。但 `login` 路由中额外检查了一次 `if not user.is_active`，这是防御性的做法，与 Flask-Login 的默认行为一致。

**（7）`auth.py` 路由中注册接口缺少事务保护——轻微**

`register` 函数中创建 User 和 PassengerProfile 两步操作，如果第二步失败（如 `id_card` 违反约束），第一步不会自动回滚。实际中 `db.session.commit()` 在最后调用，如果中间任何一步抛出异常，整个事务会回到初始状态——当前实现是正确的，但缺少显式的 `try/except` 和 `db.session.rollback()` 来处理中间失败。

### 4.3 代码风格

- 所有文件都有 `from __future__ import annotations`——一致的风格
- 类型注解覆盖率良好（函数签名、方法返回值）
- 命名遵循 Python 惯例（snake_case 用于变量和函数，PascalCase 用于类）
- 导入顺序不一致：`services.py` 中导入分散在多处，部分在文件顶部，部分在函数内部（如 `_build_no` 中未使用顶部导入但 `uuid` 和 `datetime` 已导入）

---

## 五、前端代码质量（B）

### 5.1 优点

- Vue 3 Composition API（`<script setup>`）——现代化写法。
- Pinia 状态管理——认证状态集中管理。
- Vue Router 路由守卫——`beforeEach` 中正确检查认证和角色权限。
- Element Plus 组件库——界面风格统一。
- API 拦截器统一错误处理——`api.js` 中 `response.interceptors` 提取后端错误消息。
- Vite proxy 配置正确——开发环境代理 `/api` 到后端。

### 5.2 需要关注的问题

**（1）loginForm 预填了管理员密码——轻微但存在**

```javascript
const loginForm = reactive({
  username: 'admin',
  password: 'admin123',
})
```

这是演示账号的便利功能，但对于提交到 Git 的课程设计代码来说不算大问题，系统中的三个演示账号在 README.md 中也已公开。

**（2）PassengerView 和 StaffView 存在大量代码重复——中等**

两个 Vue 文件共约 480 行代码，其中班次查询、购票/售票、退票、换票逻辑几乎完全相同。可以抽取可复用组件（如 `TripSearch.vue`、`TicketList.vue`、`SellDialog.vue`、`ExchangeDialog.vue`）来减少重复。

**（3）退票确认中的错误判断不够健壮——轻微**

```javascript
} catch (error) {
    if (error !== 'cancel') ElMessage.error(error.message)
}
```

`ElMessageBox.confirm` 在用户取消时抛出的异常需要通过 `error === 'cancel'` 或 `error instanceof Error && error.message === 'cancel'` 来判断，依赖字符串比较不够可靠。

**建议**：使用 `ElMessageBox.confirm(...).catch(() => {})` 不显示错误，或使用 `try/catch` 结合 `error === 'cancel'` 的字符串比较（当前写法在 Element Plus 中是正确的，但可读性不佳）。

**（4）Vite 依赖放在了 dependencies 而非 devDependencies**

`package.json` 中 `vite`、`@vitejs/plugin-vue` 被列在 `dependencies` 而非 `devDependencies` 中。虽然不影响构建，但不符合 Node.js 生态惯例。

**（5）缺少前端错误边界处理**

如果后端返回非预期格式的数据（如 500 错误后无 `error.message`），前端会显示"请求失败，请稍后重试。"——这是 API 拦截器的兜底逻辑，做得不错。但如果后端未启动，前端的请求会一直挂起无用户反馈。建议增加请求超时设置。

```javascript
export const api = axios.create({
  baseURL: '/api',
  withCredentials: true,
  timeout: 15000,  // 建议添加
})
```

---

## 六、测试（B）

### 6.1 现有测试覆盖

| 测试用例 | 覆盖范围 |
| --- | --- |
| `test_invalid_login_uses_standard_error_shape` | 认证失败返回标准错误格式 |
| `test_trip_search_returns_public_contract` | 班次查询返回契约字段 |
| `test_holiday_fare_rule_uses_highest_priority` | 节假日票价规则优先级选择 |
| `test_purchase_ticket_allocates_seat_and_reduces_inventory` | 购票分配座位并减少库存 |
| `test_purchase_rejects_oversell` | 拒绝超售 |
| `test_api_refund_releases_seat` | 退票释放座位 |
| `test_api_exchange_creates_new_active_ticket_and_closes_old_one` | 换票生成新车票并关闭旧票 |
| `test_admin_endpoints_require_admin_role` | 管理员权限控制 |

8 个测试用例，覆盖了核心票务流程和权限控制——这对于一个课程设计项目来说是**合理且有效的**。

### 6.2 缺少的测试

- **换票票价差额场景**：当前没有测试验证换到更高/更低票价班次时的行为（需要先修复该逻辑漏洞再测）。
- **并发购票冲突**：未测试多个用户同时购买最后一张票的场景。
- **退票后再购票**：未测试同一座位释放后能否被重新分配。
- **STF/售票员权限边界**：测试了 PASSENGER 不能访问管理接口，但未测试 STAFF 能否访问 ADMIN 专属接口。
- **注册重复用户名**：未测试注册时用户名已存在的 409 响应。
- **已停用用户登录被拒绝**：未测试 `is_active_flag=False` 时的 403 响应。

### 6.3 测试基础设施

- `TestConfig` 继承 `Config`，使用内存 SQLite——测试不依赖 MySQL，快速且隔离。
- `pytest.ini` 正确配置了 `pythonpath = backend`。
- 测试 fixture 使用 `app.app_context()` 正确设置和清理——`db.create_all()` / `db.drop_all()` 模式。
- `seed_database()` 在测试中被复用——好的做法。

---

## 七、文档与说明（A）

- README.md 完整说明了技术栈、目录结构、演示账号、本地运行步骤和验证方法。
- ADR-001-architecture.md 记录了架构决策及其理由——远超课程设计的一般要求。
- `sql/mysql_queries.sql` 包含了 4 个关键查询/事务示例——对答辩展示有帮助。
- `.gitignore` 正确排除了 `.venv`、`node_modules`、`dist`、`__pycache__`、`.env` 等。

---

## 八、汇总与优化建议

### 必须修复（答辩前）

| # | 问题 | 严重程度 | 位置 |
| --- | --- | --- | --- |
| 1 | 移除 config.py 中的 SECRET_KEY 默认值 | 安全 | `backend/app/config.py` |
| 2 | debug=True 改为环境变量控制 | 安全 | `backend/run.py` |
| 3 | 换票缺少票价差额处理 | 业务逻辑 | `backend/app/services.py` |
| 4 | 购票缺少幂等保护（重复提交防护） | 业务逻辑 | `backend/app/services.py` |

### 建议修复（有时间就改）

| # | 问题 | 位置 |
| --- | --- | --- |
| 5 | `datetime.utcnow` 替换为 `datetime.now(datetime.UTC)` | `backend/app/models.py` |
| 6 | 前端请求添加 timeout | `frontend/src/api.js` |
| 7 | 前端抽取可复用组件减少重复代码 | `frontend/src/views/` |
| 8 | Vite 插件移到 devDependencies | `frontend/package.json` |
| 9 | 补充测试用例（并发、退票后重购、注册重复等） | `backend/tests/` |
| 10 | Order 模型增加 `refunded_at` 字段 | `backend/app/models.py` |

### 答辩加分建议

| # | 建议 |
| --- | --- |
| 11 | 在报告中用 `mysql_queries.sql` 的事务示例解释数据库行锁机制 |
| 12 | 展示 ADR-001 说明为何选择 Vue+Flask 而非其他方案 |
| 13 | 在演示中展示 pytest 运行结果截图证明业务逻辑正确性 |
| 14 | 补充 ER 图（基于 `sql/mysql_schema.sql` 生成）到报告中 |

---

## 总结

该系统是一个课程设计项目中的**合格且较优秀**的作品。前后端分离架构清晰，数据库设计完整且带有合理的索引和外键约束，核心票务逻辑（购票、退票、换票、节假日浮动票价）已实现并通过测试验证。角色权限控制正确，统一错误响应格式保持了一致性。

主要短板集中在安全配置（SECRET_KEY 泄露、debug 模式）和换票业务逻辑（缺少票价差额处理）两方面。这些问题修改成本低、影响面小，在答辩前修正后，该系统可以作为一个高质量的课程设计作品进行展示和答辩。
